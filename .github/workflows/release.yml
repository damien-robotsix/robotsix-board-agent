name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: read
  actions: read

jobs:
  verify-tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false
      - name: Verify tag matches pyproject.toml version
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          TOML_VERSION=$(grep '^version = ' pyproject.toml | head -1 | sed 's/^version = "\(.*\)"/\1/')
          if [ "$TAG_VERSION" != "$TOML_VERSION" ]; then
            echo "Tag version '${TAG_VERSION}' does not match pyproject.toml version '${TOML_VERSION}'"
            exit 1
          fi
          echo "Tag version '${TAG_VERSION}' matches pyproject.toml version '${TOML_VERSION}'"

  release:
    needs: verify-tag
    permissions:
      contents: read
      id-token: write
    uses: damien-robotsix/robotsix-mill/.github/workflows/python-release.yml@b65aa6fcec7fc76e5a565c70e630623a91204f86 # main
